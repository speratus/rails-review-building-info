<section class="hero is-info">
    <div class="hero-body">
        <h1 class="title">
            Edit <%= @building.name %>
        </h1>
    </div>
</section>

<section class="section">
    <%= render 'form' %>
</section>